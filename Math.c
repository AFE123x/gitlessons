#include <stdio.h>

int multiply(int x, int y)
{
    return x * y;
}
int divide(int x, int y)
{
    return x / y;
}
int add(int a, int b)
{
    return a + b;
}
int sub(int a, int b)
{
}